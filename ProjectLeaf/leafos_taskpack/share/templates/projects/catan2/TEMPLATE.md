# catan2 Template

Created by `leafctl agent-loop-start`.

Baseline validation uses the repository Catan2 benchmark in CPU-only mode:

```text
leafctl catan2bench --profile 4m --ticks 3 --fast --quiet --provider-mode off --json
```
