# generic-python-sim Template

Created by `leafctl agent-loop-start`.

Initial validation is a deterministic skeleton check. Add Python simulation
code, tests, and reports inside the created project directory.
